\documentclass{article}%
\usepackage[T1]{fontenc}%
\usepackage[utf8]{inputenc}%
\usepackage{lmodern}%
\usepackage{textcomp}%
\usepackage{amsmath}%
%
%
%
\begin{document}%
\pagestyle{empty}%
\normalsize%
\[%
a_n = (\text{reverse}(\text{reverse}(\text{reverse}(a_{n-1}))) + \text{num\_digits}(\min((\min(\max(n, n, a_{n-1}), n, a_{n-2}) + 1), ((a_{n-3} + 1) + 1), \text{reverse}(\max(\text{reverse}(a_{n-1}), n, n)))))%
\]%
\end{document}